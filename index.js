var isSquare = function (n) {
  //   const number = 55;
  //   if (number.length / 2 == true && number.length > 0) {
  //     return n;
  //   }
  return n >= 0 && Math.sqrt(n) % 1 === 0;
  console.log(number);
};
